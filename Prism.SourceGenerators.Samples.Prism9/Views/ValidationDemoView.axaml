<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="Prism.SourceGenerators.Samples.Prism9.Views.ValidationDemoView">
  <StackPanel Margin="24" Spacing="16">
    <TextBlock Text="BindableValidator + [NotifyDataErrorInfo]"
               FontSize="20"
               FontWeight="SemiBold" />
    <TextBlock TextWrapping="Wrap"
               FontSize="13"
               Foreground="Gray"
               Text="Partial properties: DataAnnotations stay on your declaration; generator does not duplicate them on the implementing partial. TextBox errors use Fluent DataValidationErrors (ValidationResult.ErrorMessage). Lines below mirror GetErrors." />

    <StackPanel Spacing="6">
      <TextBlock Text="Username" FontWeight="SemiBold" />
      <TextBox Text="{Binding Username, Mode=TwoWay}" Watermark="2+ characters" />
    </StackPanel>

    <StackPanel Spacing="6">
      <TextBlock Text="Email" FontWeight="SemiBold" />
      <TextBox Text="{Binding Email, Mode=TwoWay}" Watermark="name@example.com" />
    </StackPanel>

    <TextBlock Text="{Binding HasErrorsSummary}" FontSize="13" />

    <StackPanel Orientation="Horizontal" Spacing="12">
      <Button Content="Validate all properties" Command="{Binding ValidateAllCommand}" />
      <Button Content="Clear all errors" Command="{Binding ClearAllCommand}" />
    </StackPanel>
  </StackPanel>
</UserControl>

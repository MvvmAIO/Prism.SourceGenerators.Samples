# Platform samples CI matrix

- **ubuntu-latest** — Avalonia (Prism 8/9), MAUI ViewModel library, Uno/WinUI compatibility library
- **windows-latest** — WPF shell app (requires `net8.0-windows`)

WPF is excluded from the Linux job via explicit project list.
